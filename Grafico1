avg_access = (
    df.groupby("Entity")["Access to electricity (% of population)"]
      .mean()
      .reset_index()
      .sort_values(by="Access to electricity (% of population)", ascending=False)
)

# Tomar los 10 países con mayor acceso promedio
top10 = avg_access.head(10)

# Crear el gráfico
plt.figure(figsize=(12,6))
ax = sns.barplot(
    data=top10,
    x="Entity",
    y="Access to electricity (% of population)",
    palette="viridis"
)

# Mostrar los valores exactos con 3 decimales
for i, value in enumerate(top10["Access to electricity (% of population)"]):
    ax.text(i, value + 0.05, f"{value:.3f}%", ha='center', va='bottom', fontsize=10)

# Personalización
plt.title("Top 10 países con mayor acceso promedio a electricidad", fontsize=14, weight='bold')
plt.xlabel("País")
plt.ylabel("Acceso promedio a electricidad (%)")

# Ajustar eje Y para ver pequeñas diferencias
plt.ylim(99, 100.1)  # 🔹 Zoom en la parte alta para apreciar las diferencias decimales
plt.xticks(rotation=45, ha='right')
plt.grid(axis='y', linestyle='--', alpha=0.7)
plt.tight_layout()
plt.show()
